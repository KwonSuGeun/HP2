SET HEADING OFF
SET FEEDBACK OFF
SET VERIFY OFF
SET PAGESIZE 0

SELECT 'DIFF_CNT=' || COUNT(*)
  FROM HIS_MENU
 WHERE MENU_NAME <> MENU_NAME_EN;

SELECT 'KO_SAMPLE_MATCH=' || COUNT(*)
  FROM HIS_MENU
 WHERE MENU_CODE = 'RECEPTION'
   AND MENU_NAME = UNISTR('\C811\C218');

EXIT;
